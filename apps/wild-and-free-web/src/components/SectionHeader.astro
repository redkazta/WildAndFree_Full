---
export type Props = {
  title: string;
  subtitle?: string;
  label?: string;
};

const { title, subtitle, label } = Astro.props;
---

<div class="text-center space-y-4 mb-section">
  {label && (
    <span class="text-label font-black uppercase text-wild-orange tracking-widest">
      {label}
    </span>
  )}
  <h2 class="text-display-md font-black uppercase tracking-tighter text-[var(--text)] relative inline-block">
    {title}
  </h2>
  {subtitle && (
    <p class="text-label font-bold text-[var(--text-muted)] uppercase max-w-content mx-auto">
      {subtitle}
    </p>
  )}
</div>
