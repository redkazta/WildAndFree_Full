---
import Layout from '../layouts/Layout.astro';
import tenantConfig from '../../../../packages/shared-types/tenant-config.json';

interface Props { typeLabel?: string }
const { typeLabel = 'publicación' } = Astro.props;

const tenant = tenantConfig['wild-and-free'];
const config = {
  name: "Wild Gvng - THE WILD TIMES",
  primary_color: tenant.primary_color,
  secondary_color: tenant.secondary_color
};
---

<Layout config={config}>
  <div class="min-h-screen bg-[var(--bg)] text-[var(--text)] font-sans pb-20">

    <header class="border-b-2 border-[var(--border)] py-6 px-4 md:px-8">
      <div class="max-w-[1400px] mx-auto">
        <div class="flex justify-between items-end border-b border-[var(--border)] pb-2 mb-2 text-[10px] font-bold uppercase tracking-[0.2em] text-[var(--text-muted)]">
          <span>Vol. 1 — Edición Digital</span>
          <span>Torreón, Coahuila, MX</span>
          <span class="text-[var(--primary)]">GRATIS</span>
        </div>
        <h1 class="text-5xl md:text-7xl lg:text-8xl font-black tracking-tighter uppercase leading-[0.85] text-[var(--text)]">
          THE WILD <span class="text-[var(--primary)]">TIMES</span>
        </h1>
        <div class="flex items-center gap-4 mt-2 border-t border-[var(--border)] pt-2">
          <span class="bg-[var(--primary)] text-[var(--bg)] px-3 py-0.5 text-[10px] font-black uppercase tracking-wider">Fanzine Oficial</span>
          <span class="text-[10px] font-bold uppercase tracking-[0.2em] text-[var(--text-muted)]">Noticias · Cultura · Música · Calle</span>
        </div>
      </div>
    </header>

    <div class="nf-wrap px-4 md:px-8">
      <div class="nf-inner">
        <nav class="nf-back mb-6">
          <a href="/the-wild-times" class="nf-back-link">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M19 12H5"/><path d="M12 19l-7-7 7-7"/></svg>
            Volver a The Wild Times
          </a>
        </nav>
        <div class="nf-card">
          <span class="nf-emoji">🔍</span>
          <h2 class="nf-title">No se encontró la {typeLabel}</h2>
          <p class="nf-text">Puede que haya sido eliminada o que el enlace no sea correcto.</p>
          <a href="/the-wild-times" class="nf-btn">Ir al inicio</a>
        </div>
      </div>
    </div>
  </div>
</Layout>

<style is:global>
  .nf-wrap { max-width: 1200px; margin: 0 auto; }
  .nf-inner { max-width: 640px; margin: 0 auto; padding: 60px 0; }
  .nf-back { display: flex; align-items: center; gap: 8px; }
  .nf-back-link {
    display: inline-flex; align-items: center; gap: 8px;
    font-size: 12px; font-weight: 800; text-transform: uppercase; letter-spacing: 0.06em;
    color: var(--text-muted); text-decoration: none; transition: color 0.2s ease;
  }
  .nf-back-link:hover { color: var(--primary); }
  .nf-card {
    text-align: center; padding: 48px 32px; border-radius: 20px;
    background: var(--bg-card); border: 1px solid var(--border);
  }
  .nf-emoji { font-size: 48px; display: block; margin-bottom: 16px; }
  .nf-title { font-size: 24px; font-weight: 900; text-transform: uppercase; margin: 0 0 8px; }
  .nf-text { font-size: 14px; color: var(--text-muted); margin: 0 0 24px; }
  .nf-btn {
    display: inline-block; padding: 12px 28px; border-radius: 12px;
    background: var(--primary); color: var(--bg); font-size: 12px; font-weight: 900;
    text-transform: uppercase; letter-spacing: 0.05em; text-decoration: none;
  }
</style>
