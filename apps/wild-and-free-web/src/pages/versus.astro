---
import Layout from '../layouts/Layout.astro';
import { supabase } from '../lib/supabase';
import tenantConfig from '../../../../packages/shared-types/tenant-config.json';

// --- DATA FETCHING ---
let dbArtists = null;
try {
  const { data, error } = await supabase
    .from('artists')
    .select('*')
    .order('nombre');
  if (!error) dbArtists = data;
} catch (e) {
  console.warn('Could not fetch artists from Supabase during build.');
}

// Fallback / Mock Data enriched with "Special Moves" (Tracks)
const mockArtists = [
  {
    id: 1,
    nombre: 'Rayos X',
    slug: 'rayos-x',
    imagenes: { profile: 'https://images.unsplash.com/photo-1516280440614-37939bbacd81?w=800&q=80' },
    stats: { seguidores: '1.2M', oyentes: '850K', power: 88, speed: 92, technique: 75 },
    bio: 'El relámpago del norte. Sus rimas cortan como el cristal y su flow es pura electricidad.',
    tracks: ['Voltaje Máximo', 'Cero Grados', 'Norteño Flow', 'Rayos y Centellas'],
    estilo: 'Rap Eléctrico',
    origen: 'Monterrey, NL'
  },
  {
    id: 2,
    nombre: 'Sombra 47',
    slug: 'sombra-47',
    imagenes: { profile: 'https://images.unsplash.com/photo-1521119989659-a83eee488004?w=800&q=80' },
    stats: { seguidores: '2.5M', oyentes: '1.8M', power: 95, speed: 85, technique: 90 },
    bio: 'Directo de los callejones más oscuros de la CDMX. La sombra que camina sola.',
    tracks: ['Callejón Sin Salida', 'Sombra Eterna', 'Distrito Federal', '47 Balas'],
    estilo: 'Trap Oscuro',
    origen: 'CDMX, MX'
  },
  {
    id: 3,
    nombre: 'Fuego Lírico',
    slug: 'fuego-lirico',
    imagenes: { profile: 'https://images.unsplash.com/photo-1492562080023-ab3db95bfbce?w=800&q=80' },
    stats: { seguidores: '800K', oyentes: '1.1M', power: 82, speed: 96, technique: 88 },
    bio: 'Incendiando cada escenario que pisa. Sus letras queman a la competencia.',
    tracks: ['Llama Eterna', 'Incendio Musical', 'Calor de Barrio', 'Fuego en la Sangre'],
    estilo: 'Freestyle',
    origen: 'Guadalajara, JAL'
  },
  {
    id: 4,
    nombre: 'Diamante Negro',
    slug: 'diamante-negro',
    imagenes: { profile: 'https://images.unsplash.com/photo-1500648767791-00dcc994a43e?w=800&q=80' },
    stats: { seguidores: '4.2M', oyentes: '3.5M', power: 90, speed: 80, technique: 95 },
    bio: 'Brillante bajo presión. La joya más valiosa del hip-hop nacional.',
    tracks: ['Presión Extrema', 'Brillo Eterno', 'Lujo Callejero', 'Diamante en Bruto'],
    estilo: 'Luxury Rap',
    origen: 'Tijuana, BC'
  },
  {
    id: 5,
    nombre: 'Neon Kid',
    slug: 'neon-kid',
    imagenes: { profile: 'https://images.unsplash.com/photo-1539571696357-5a69c17a67c6?w=800&q=80' },
    stats: { seguidores: '3.7M', oyentes: '4.2M', power: 85, speed: 98, technique: 92 },
    bio: 'La luz que nunca se apaga. Ritmos futuristas para una generación sin sueño.',
    tracks: ['Ciudad Neón', 'Luces de Ciudad', 'Cyber Flow', 'Noche Eterna'],
    estilo: 'Synth Trap',
    origen: 'Puebla, MX'
  },
  {
    id: 6,
    nombre: 'Relámpago',
    slug: 'relampago',
    imagenes: { profile: 'https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=800&q=80' },
    stats: { seguidores: '1.8M', oyentes: '2.1M', power: 94, speed: 99, technique: 80 },
    bio: 'Más rápido que el sonido. Antes de que lo veas, ya te ha derrotado.',
    tracks: ['Velocidad Terminal', 'Trueno Callejero', 'Impacto Crítico', 'Sin Rastro'],
    estilo: 'Speed Flow',
    origen: 'Juárez, CHIH'
  },
  {
    id: 7,
    nombre: 'Garras',
    slug: 'garras',
    imagenes: { profile: 'https://images.unsplash.com/photo-1534030347209-7147fd9e791a?w=800&q=80' },
    stats: { seguidores: '5.2M', oyentes: '3.9M', power: 98, speed: 75, technique: 85 },
    bio: 'Desgarrando el beat con cada verso. Un animal salvaje en el estudio.',
    tracks: ['Instinto Animal', 'Garras Afiladas', 'Caza Nocturna', 'Selva de Asfalto'],
    estilo: 'Hardcore Rap',
    origen: 'Hermosillo, SON'
  },
  {
    id: 8,
    nombre: 'Tormenta',
    slug: 'tormenta',
    imagenes: { profile: 'https://images.unsplash.com/photo-1504257432389-52343af06ae3?w=800&q=80' },
    stats: { seguidores: '950K', oyentes: '600K', power: 92, speed: 88, technique: 80 },
    bio: 'Energía pura de Gómez Palacio. Imparable como un huracán.',
    tracks: ['Golpe de Trueno', 'Velocidad del Rayo', 'Alerta de Tormenta', 'Lluvia Pesada'],
    estilo: 'Hardcore Rap',
    origen: 'Gómez Palacio, DGO'
  },
  {
    id: 9,
    nombre: 'Víbora X',
    slug: 'vibora-x',
    imagenes: { profile: 'https://images.unsplash.com/photo-1500917293891-ef795e70e1f6?w=800&q=80' },
    stats: { seguidores: '3.1M', oyentes: '2.8M', power: 96, speed: 99, technique: 95 },
    bio: 'El veneno lírico más potente del norte. Nadie sobrevive a su tiradera.',
    tracks: ['Mordida de Veneno', 'Ojos de Serpiente', 'Flow Tóxico', 'Golpe Mortal'],
    estilo: 'Fast Flow',
    origen: 'Chihuahua, MX'
  },
  {
    id: 10,
    nombre: 'Luna Negra',
    slug: 'luna-negra',
    imagenes: { profile: 'https://images.unsplash.com/photo-1531123897727-8f129e16fd3c?w=800&q=80' },
    stats: { seguidores: '1.9M', oyentes: '2.2M', power: 85, speed: 90, technique: 92 },
    bio: 'Voces etéreas sobre bajos pesados. La bruja del R&B.',
    tracks: ['Luz de Luna', 'Cielo Oscuro', 'Eclipse Total', 'Susurro Nocturno'],
    estilo: 'Dark R&B',
    origen: 'Veracruz, MX'
  },
  {
    id: 11,
    nombre: 'Rojo Fuego',
    slug: 'rojo-fuego',
    imagenes: { profile: 'https://images.unsplash.com/photo-1506794778202-cad84cf45f1d?w=800&q=80' },
    stats: { seguidores: '500K', oyentes: '800K', power: 94, speed: 96, technique: 85 },
    bio: 'La nueva promesa del trap duro. Incendiando escenarios.',
    tracks: ['Iniciador de Fuego', 'Al Rojo Vivo', 'Quémalo Todo', 'Infierno'],
    estilo: 'Trap Metal',
    origen: 'Ecatepec, MX'
  },
  {
    id: 12,
    nombre: 'Ciber Punk',
    slug: 'ciber-punk',
    imagenes: { profile: 'https://images.unsplash.com/photo-1519085360753-af0119f7cbe7?w=800&q=80' },
    stats: { seguidores: '4.5M', oyentes: '5.1M', power: 90, speed: 92, technique: 99 },
    bio: 'Sonidos del futuro hoy. Hackeando el sistema musical.',
    tracks: ['Ciudad Futura', 'Alma Cibernética', 'Ritmo Tech', 'Lluvia Neón'],
    estilo: 'Glitch Hop',
    origen: 'Zapopan, MX'
  },
  {
    id: 13,
    nombre: 'Fantasma G',
    slug: 'fantasma-g',
    imagenes: { profile: 'https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=800&q=80' },
    stats: { seguidores: '1.4M', oyentes: '980K', power: 88, speed: 94, technique: 90 },
    bio: 'El espectro del beat, apareciendo donde menos lo esperas.',
    tracks: ['Modo Fantasma', 'Golpe Invisible', 'Dolor Fantasma', 'Mundo Espiritual'],
    estilo: 'Horrorcore',
    origen: 'Toluca, MX'
  },
  {
    id: 14,
    nombre: 'Micrófono de Hierro',
    slug: 'microfono-de-hierro',
    imagenes: { profile: 'https://images.unsplash.com/photo-1500648767791-00dcc994a43e?w=800&q=80' },
    stats: { seguidores: '3.2M', oyentes: '2.9M', power: 99, speed: 70, technique: 85 },
    bio: 'Barras de acero y voluntad inquebrantable. Peso pesado.',
    tracks: ['Voluntad de Hierro', 'Metal Pesado', 'Barras de Acero', 'Óxido y Polvo'],
    estilo: 'Boom Bap',
    origen: 'Torreón, COAH'
  },
  {
    id: 15,
    nombre: 'Rosa de Terciopelo',
    slug: 'rosa-de-terciopelo',
    imagenes: { profile: 'https://images.unsplash.com/photo-1494790108377-be9c29b29330?w=800&q=80' },
    stats: { seguidores: '4.1M', oyentes: '5.5M', power: 80, speed: 90, technique: 98 },
    bio: 'Suavidad que mata. R&B con espinas.',
    tracks: ['Muerte Suave', 'Pinchazo de Espina', 'Pétalos Rojos', 'Camino de Seda'],
    estilo: 'Soul Trap',
    origen: 'Mérida, MX'
  },
  {
    id: 16,
    nombre: 'Tóxico B',
    slug: 'toxico-b',
    imagenes: { profile: 'https://images.unsplash.com/photo-1521119989659-a83eee488004?w=800&q=80' },
    stats: { seguidores: '2.0M', oyentes: '1.5M', power: 92, speed: 95, technique: 88 },
    bio: 'El veneno está en la dosis. Trap ácido que corroe.',
    tracks: ['Lluvia Ácida', 'Amor Tóxico', 'Hiedra Venenosa', 'Peligro'],
    estilo: 'Acid Rap',
    origen: 'León, MX'
  },
  {
    id: 17,
    nombre: 'Llamarada Solar',
    slug: 'llamarada-solar',
    imagenes: { profile: 'https://images.unsplash.com/photo-1492562080023-ab3db95bfbce?w=800&q=80' },
    stats: { seguidores: '1.1M', oyentes: '800K', power: 95, speed: 85, technique: 90 },
    bio: 'Brillando más fuerte que el sol de mediodía. Cega a la competencia.',
    tracks: ['Quemadura', 'Ola de Calor', 'Poder Solar', 'Luz Cegadora'],
    estilo: 'Reggaetón',
    origen: 'Cancún, MX'
  },
  {
    id: 18,
    nombre: 'Hielo Seco',
    slug: 'hielo-seco',
    imagenes: { profile: 'https://images.unsplash.com/photo-1506794778202-cad84cf45f1d?w=800&q=80' },
    stats: { seguidores: '1.9M', oyentes: '2.1M', power: 85, speed: 92, technique: 96 },
    bio: 'Flow helado que congela la pista. Sangre fría.',
    tracks: ['Congelación', 'Cero Grados', 'Era de Hielo', 'Glaciar'],
    estilo: 'Drill',
    origen: 'Saltillo, MX'
  },
  {
    id: 19,
    nombre: 'La Bestia',
    slug: 'la-bestia',
    imagenes: { profile: 'https://images.unsplash.com/photo-1539571696357-5a69c17a67c6?w=800&q=80' },
    stats: { seguidores: '5.0M', oyentes: '6.2M', power: 99, speed: 85, technique: 80 },
    bio: 'Fuerza bruta desatada. Rompiendo bocinas desde el 2010.',
    tracks: ['Modo Bestia', 'Rugido', 'Fuerza Bruta', 'Sin Cadenas'],
    estilo: 'Hardcore',
    origen: 'Culiacán, MX'
  },
  {
    id: 20,
    nombre: 'Karmma',
    slug: 'karmma',
    imagenes: { profile: 'https://images.unsplash.com/photo-1524504388940-b1c1722653e1?w=800&q=80' },
    stats: { seguidores: '800K', oyentes: '1.5M', power: 88, speed: 94, technique: 97 },
    bio: 'Lo que das, recibes. Barras que regresan para golpearte.',
    tracks: ['Causa y Efecto', 'Justicia Divina', 'Ciclo Sin Fin', 'Destino'],
    estilo: 'Consciente',
    origen: 'Morelia, MX'
  }
];

// If DB returns data, we might need to map it or merge with mock for "tracks" and "stats" if missing
const artists = (dbArtists && dbArtists.length > 0) ? dbArtists.map((a: any, i: number) => ({
    ...a,
    // Ensure missing fields have defaults
    stats: a.stats || mockArtists[i % mockArtists.length].stats,
    tracks: mockArtists[i % mockArtists.length].tracks,
    estilo: mockArtists[i % mockArtists.length].estilo,
    origen: mockArtists[i % mockArtists.length].origen
})) : mockArtists;

const tenant = tenantConfig['wild-and-free'];
const config = {
  name: "Wild Gvng - Versus Mode",
  primary_color: tenant.primary_color,
  secondary_color: tenant.secondary_color
};
---

<Layout config={config}>
  <div id="page-container" class="min-h-screen bg-[#0a0a0a] text-white font-sans overflow-x-hidden relative selection:bg-[var(--wild-orange)] selection:text-black pb-48">
    
    <!-- ARCADE BACKGROUND -->
    <div class="absolute inset-0 bg-[url('https://www.transparenttextures.com/patterns/carbon-fibre.png')] opacity-20 pointer-events-none z-0"></div>
    <div class="absolute inset-0 bg-gradient-to-b from-black via-black/90 to-black pointer-events-none z-0"></div>
    
    <!-- TOP BAR -->
    <div class="relative z-50 bg-black/90 backdrop-blur-md border-b border-white/10 py-3">
        <div class="container mx-auto px-4 flex justify-between items-center">
            <div class="flex items-center gap-3">
                <div class="w-2 h-2 bg-[var(--primary)] animate-pulse rounded-full shadow-[0_0_10px_var(--primary)]"></div>
                <span class="text-[10px] font-black uppercase tracking-[0.2em] text-white arcade-font">VERSUS MODE // SYSTEM V2.0</span>
            </div>
            <div class="text-[10px] font-mono text-gray-500 font-bold uppercase hidden md:block">BATTLE ARENA ONLINE</div>
        </div>
    </div>

    <!-- MAIN ARENA CONTAINER -->
    <div class="relative z-10 container mx-auto px-4 py-8 flex flex-col items-center">
      
      <!-- HEADER & MODE SELECTOR -->
      <div class="text-center animate-fade-in-down mb-10 w-full max-w-4xl flex flex-col items-center gap-6">
          <h1 class="text-5xl md:text-8xl font-black italic uppercase tracking-tighter text-transparent bg-clip-text bg-gradient-to-r from-white via-[var(--primary)] to-white drop-shadow-[0_0_30px_rgba(255,255,255,0.2)] arcade-title">
              VERSUS
          </h1>
          
          <!-- MODE SELECTOR (Compact) -->
          <div class="flex gap-2 p-1 bg-white/5 rounded-full border border-white/10 backdrop-blur-sm">
              <button class="mode-btn px-6 py-2 rounded-full text-[10px] font-black uppercase tracking-widest transition-all hover:bg-white/10 text-gray-500 active" data-mode="1v1">1 VS 1</button>
              <button class="mode-btn px-6 py-2 rounded-full text-[10px] font-black uppercase tracking-widest transition-all hover:bg-white/10 text-gray-500" data-mode="2v2">2 VS 2</button>
              <button class="mode-btn px-6 py-2 rounded-full text-[10px] font-black uppercase tracking-widest transition-all hover:bg-white/10 text-gray-500" data-mode="3v3">3 VS 3</button>
          </div>
      </div>

      <!-- BATTLE RING LAYOUT (SYMMETRICAL) -->
      <div class="w-full max-w-6xl grid grid-cols-1 md:grid-cols-3 gap-8 items-center justify-center relative">
          
          <!-- LEFT: TEAM ALPHA -->
          <div class="flex flex-col items-center gap-6 order-2 md:order-1">
              <div class="text-2xl font-black text-white/10 uppercase tracking-tighter italic border-b-2 border-[var(--primary)] px-8 pb-1 transform -skew-x-12">TEAM ALPHA</div>
              <div id="team-alpha-slots" class="flex flex-col gap-4">
                  <!-- Slots injected via JS -->
              </div>
          </div>

          <!-- CENTER: VS & ACTION -->
          <div class="flex flex-col items-center justify-center gap-8 order-1 md:order-2 py-10">
              <!-- VS CIRCLE -->
              <div class="relative group cursor-default">
                  <div class="absolute inset-0 bg-[var(--primary)] rounded-full blur-[50px] opacity-20 group-hover:opacity-40 transition-opacity animate-pulse"></div>
                  <div class="w-32 h-32 md:w-40 md:h-40 bg-black border-4 border-[var(--primary)] rounded-full flex items-center justify-center relative z-10 shadow-[0_0_30px_rgba(0,0,0,0.5)]">
                      <span class="text-6xl md:text-7xl font-black italic text-white tracking-tighter transform -rotate-12 drop-shadow-[4px_4px_0px_var(--primary)]">VS</span>
                  </div>
              </div>

              <!-- FIGHT BUTTON -->
              <button id="fight-btn" class="group relative px-12 py-4 bg-white text-black font-black uppercase tracking-[0.2em] italic rounded-xl overflow-hidden transition-all hover:scale-105 active:scale-95 shadow-[0_0_30px_rgba(255,255,255,0.1)] hover:shadow-[0_0_50px_var(--primary)]">
                  <span class="relative z-10 text-xl">FIGHT!</span>
                  <div class="absolute inset-0 bg-gradient-to-r from-[var(--primary)] via-white to-[var(--primary)] opacity-0 group-hover:opacity-100 transition-opacity"></div>
              </button>

              <!-- INFO DISPLAY (Placeholder for selected artist info) -->
              <div id="center-info" class="text-center opacity-50 h-12 flex flex-col justify-end">
                  <p class="text-[8px] font-mono uppercase tracking-widest text-gray-500">Selecciona un artista para ver stats</p>
              </div>
          </div>

          <!-- RIGHT: TEAM OMEGA -->
          <div class="flex flex-col items-center gap-6 order-3">
              <div class="text-2xl font-black text-white/10 uppercase tracking-tighter italic border-b-2 border-red-600 px-8 pb-1 transform skew-x-12">TEAM OMEGA</div>
              <div id="team-omega-slots" class="flex flex-col gap-4">
                  <!-- Slots injected via JS -->
              </div>
          </div>

      </div>

      <!-- BRACKETS SECTION (Moved down) -->
      <div class="mt-24 w-full max-w-5xl border-t border-white/5 pt-12">
          <div class="flex items-center justify-between mb-8">
              <h3 class="text-xl font-black uppercase tracking-widest text-white/40">Torneo Generado</h3>
              <button id="regenerate-brackets" class="text-[10px] font-bold text-[var(--primary)] uppercase tracking-widest hover:text-white transition-colors flex items-center gap-2">
                  <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path></svg>
                  Recalcular
              </button>
          </div>
          <div id="brackets-container" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
              <!-- Brackets injected via JS -->
          </div>
      </div>

    </div>

    <!-- ARTIST PICKER DOCK (Floating Bottom) -->
    <div id="artist-dock" class="fixed bottom-0 left-0 right-0 z-[100] bg-black/90 backdrop-blur-xl border-t border-white/10 p-4 transform transition-transform duration-500 shadow-[0_-20px_50px_rgba(0,0,0,0.5)]">
        <div class="container mx-auto">
            <div class="flex items-center justify-between mb-3 px-2">
                <div class="flex items-center gap-3">
                    <div class="w-1.5 h-1.5 bg-[var(--primary)] rounded-full animate-ping"></div>
                    <h3 class="text-[10px] font-black text-white uppercase tracking-[0.3em] arcade-font">Crew Disponible</h3>
                </div>
                <div class="flex items-center gap-4">
                    <span class="text-[8px] font-bold text-white/30 uppercase tracking-[0.2em] hidden md:block">Arrastra artistas a los slots de combate</span>
                    <button id="toggle-dock" class="w-8 h-8 rounded-full bg-white/5 flex items-center justify-center text-white/50 hover:text-white hover:bg-white/10 transition-all border border-white/10">
                        <svg id="dock-icon" class="w-4 h-4 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
                    </button>
                </div>
            </div>
            
            <div id="dock-content" class="flex gap-4 overflow-x-auto pb-4 pt-2 px-2 custom-scrollbar mask-fade-edges justify-center md:justify-start">
                 {artists.map((artist: any) => (
                     <div 
                       class="artist-token flex-shrink-0 w-20 h-20 md:w-24 md:h-24 relative group cursor-grab active:cursor-grabbing border-2 border-white/5 hover:border-[var(--primary)] hover:scale-110 transition-all duration-300 overflow-hidden bg-zinc-900 rounded-xl shadow-2xl"
                       draggable="true"
                       data-id={artist.id}
                       data-name={artist.nombre}
                       data-role={artist.estilo}
                       data-stats={JSON.stringify(artist.stats)}
                     >
                         <img src={artist.imagenes.profile} class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-500" loading="lazy" />
                         <div class="absolute inset-0 bg-gradient-to-t from-black via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity flex items-end justify-center p-2">
                             <span class="text-[7px] font-black text-white uppercase text-center leading-none truncate w-full tracking-tighter">{artist.nombre}</span>
                         </div>
                         <div class="absolute top-1 right-1 w-2 h-2 bg-[var(--primary)] rounded-full opacity-0 group-hover:opacity-100 transition-opacity shadow-[0_0_10px_var(--primary)]"></div>
                     </div>
                 ))}
             </div>
        </div>
    </div>

  </div>
</Layout>

<!-- CLIENT SIDE LOGIC -->
<script define:vars={{ artists }}>
    const arena = document.getElementById('page-container');
    const centerInfo = document.getElementById('center-info');

    // DOCK TOGGLE LOGIC
    const dock = document.getElementById('artist-dock');
    const toggleBtn = document.getElementById('toggle-dock');
    const dockIcon = document.getElementById('dock-icon');
    let isDockOpen = true;

    if (toggleBtn && dock) {
        toggleBtn.addEventListener('click', () => {
            isDockOpen = !isDockOpen;
            if (isDockOpen) {
                dock.style.transform = 'translateY(0)';
                dockIcon.style.transform = 'rotate(0)';
            } else {
                dock.style.transform = 'translateY(calc(100% - 50px))';
                dockIcon.style.transform = 'rotate(180deg)';
            }
        });
    }

    // DRAG & INFO LOGIC
    const tokens = document.querySelectorAll('.artist-token');

    function showArtistInfo(name, role, stats) {
        if (!centerInfo) return;
        centerInfo.innerHTML = `
            <div class="animate-fade-in-up">
                <h4 class="text-lg font-black italic uppercase text-white tracking-tighter leading-none">${name}</h4>
                <p class="text-[9px] font-bold text-[var(--primary)] uppercase tracking-[0.3em] mb-1">${role}</p>
                <div class="flex gap-2 justify-center text-[8px] font-mono text-gray-500">
                    <span>PWR:${stats.power}</span>
                    <span>SPD:${stats.speed}</span>
                    <span>TEC:${stats.technique}</span>
                </div>
            </div>
        `;
        centerInfo.classList.remove('opacity-50');
    }

    function initDragEvents() {
        const tokens = document.querySelectorAll('.artist-token');
        tokens.forEach(token => {
            token.addEventListener('dragstart', (e) => {
                const id = token.getAttribute('data-id');
                e.dataTransfer.setData('text/plain', id);
                e.dataTransfer.effectAllowed = 'copy';
                
                // Highlight empty slots
                document.querySelectorAll('.team-slot:not(.filled)').forEach(s => {
                    s.classList.add('border-[var(--primary)]/50', 'animate-pulse');
                });
            });

            token.addEventListener('dragend', () => {
                document.querySelectorAll('.team-slot').forEach(s => {
                    s.classList.remove('border-[var(--primary)]/50', 'animate-pulse');
                });
            });

            token.addEventListener('click', () => {
                const name = token.getAttribute('data-name');
                const role = token.getAttribute('data-role');
                const stats = JSON.parse(token.getAttribute('data-stats'));
                showArtistInfo(name, role, stats);
                
                // Visual feedback
                tokens.forEach(t => t.classList.remove('ring-2', 'ring-[var(--primary)]'));
                token.classList.add('ring-2', 'ring-[var(--primary)]');
            });
        });
    }

    initDragEvents();

    // TEAM SLOTS LOGIC
    function fillTeamSlot(slot, artistId) {
        // Check duplicate across all slots
        const isAlreadySelected = Array.from(document.querySelectorAll('.team-slot')).some(s => s.getAttribute('data-artist-id') == artistId);
        if (isAlreadySelected) return;

        const artist = artists.find(a => a.id == artistId);
        if (!artist) return;

        const contentDiv = slot.querySelector('.slot-content');
        slot.querySelector('.placeholder-text').classList.add('hidden');
        contentDiv.classList.remove('hidden');
        
        // Use the same square size and style as the dock tokens
        contentDiv.innerHTML = `
            <div class="w-full h-full relative group overflow-hidden bg-zinc-900 animate-fade-in">
                <img src="${artist.imagenes.profile}" class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-500" />
                <div class="absolute inset-0 bg-gradient-to-t from-black via-transparent to-transparent opacity-80"></div>
                <div class="absolute bottom-0 left-0 right-0 p-1 text-center bg-black/60 backdrop-blur-sm">
                    <span class="text-[7px] font-black text-white uppercase tracking-tighter leading-none block truncate">${artist.nombre}</span>
                </div>
                <button class="remove-btn absolute top-1 right-1 w-4 h-4 bg-red-600 rounded-full text-white flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity hover:scale-110">
                    <svg class="w-2 h-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M6 18L18 6M6 6l12 12"></path></svg>
                </button>
            </div>
        `;
        
        slot.classList.add('filled', 'border-[var(--primary)]');
        slot.setAttribute('data-artist-id', artistId);

        // Remove listener
        const removeBtn = contentDiv.querySelector('.remove-btn');
        if (removeBtn) {
            removeBtn.addEventListener('click', (e) => {
                e.stopPropagation();
                clearSlot(slot);
            });
        }
        
        // Show info on click
        slot.addEventListener('click', () => {
             showArtistInfo(artist.nombre, artist.estilo, artist.stats);
        });

        checkTeamStatus();
    }

    function clearSlot(slot) {
        slot.removeAttribute('data-artist-id');
        slot.classList.remove('filled', 'border-[var(--primary)]');
        slot.querySelector('.slot-content').innerHTML = '';
        slot.querySelector('.slot-content').classList.add('hidden');
        slot.querySelector('.placeholder-text').classList.remove('hidden');
        checkTeamStatus();
    }

    function checkTeamStatus() {
        const filledSlots = document.querySelectorAll('.team-slot[data-artist-id]').length;
        const totalNeeded = document.querySelectorAll('.team-slot').length;
        const fightBtn = document.getElementById('fight-btn');
        
        if (filledSlots === totalNeeded && totalNeeded > 0) {
            fightBtn.classList.remove('grayscale', 'pointer-events-none', 'opacity-50');
            fightBtn.classList.add('animate-pulse');
        } else {
            // fightBtn.classList.add('grayscale', 'pointer-events-none', 'opacity-50');
            // fightBtn.classList.remove('animate-pulse');
            // Optional: Disable fight button until full
        }
    }

    // MODE SELECTION
    const modeBtns = document.querySelectorAll('.mode-btn');
    modeBtns.forEach(btn => {
        btn.addEventListener('click', () => {
            modeBtns.forEach(b => {
                b.classList.remove('active', 'bg-[var(--primary)]', 'text-black');
                b.classList.add('text-gray-500');
            });
            btn.classList.add('active', 'bg-[var(--primary)]', 'text-black');
            btn.classList.remove('text-gray-500');
            
            const mode = btn.getAttribute('data-mode');
            initSlots(mode);
            generateBrackets(mode);
        });
    });

    function initSlots(mode = '1v1') {
        const containers = {
            alpha: document.getElementById('team-alpha-slots'),
            omega: document.getElementById('team-omega-slots')
        };
        if (!containers.alpha || !containers.omega) return;

        const count = mode === '3v3' ? 3 : mode === '2v2' ? 2 : 1;
        
        ['alpha', 'omega'].forEach(team => {
            containers[team].innerHTML = '';
            for (let i = 0; i < count; i++) {
                const slot = createSlotElement(i + 1, team);
                containers[team].appendChild(slot);
                setupSlotEvents(slot);
            }
        });
        checkTeamStatus();
    }

    function createSlotElement(index, team) {
        const slot = document.createElement('div');
        // Square slots matching dock token size: w-20 h-20 md:w-24 md:h-24
        slot.className = `team-slot group/slot relative w-20 h-20 md:w-24 md:h-24 bg-white/5 border-2 border-white/10 rounded-xl flex items-center justify-center cursor-pointer transition-all hover:border-[var(--primary)]/50 overflow-hidden`;
        slot.innerHTML = `
            <span class="placeholder-text text-[8px] font-black text-white/10 uppercase tracking-widest z-10 pointer-events-none select-none">${team} ${index}</span>
            <div class="slot-content absolute inset-0 hidden w-full h-full"></div>
        `;
        return slot;
    }

    function setupSlotEvents(slot) {
        slot.addEventListener('dragover', (e) => e.preventDefault());
        slot.addEventListener('dragenter', () => slot.classList.add('bg-[var(--primary)]/10', 'border-[var(--primary)]/50'));
        slot.addEventListener('dragleave', () => slot.classList.remove('bg-[var(--primary)]/10', 'border-[var(--primary)]/50'));
        slot.addEventListener('drop', (e) => {
            e.preventDefault();
            slot.classList.remove('bg-[var(--primary)]/10', 'border-[var(--primary)]/50');
            const id = e.dataTransfer.getData('text/plain');
            if (id) fillTeamSlot(slot, id);
        });
    }

    // BRACKETS
    const bracketsContainer = document.getElementById('brackets-container');
    const regenerateBtn = document.getElementById('regenerate-brackets');

    function generateBrackets(mode = '1v1') {
        if (!bracketsContainer) return;
        bracketsContainer.innerHTML = '';
        const shuffled = [...artists].sort(() => 0.5 - Math.random());
        const participants = mode === '3v3' ? 3 : mode === '2v2' ? 2 : 1;
        
        for (let i = 0; i < 4; i++) {
            const tA = shuffled.slice(i * participants * 2, i * participants * 2 + participants);
            const tB = shuffled.slice(i * participants * 2 + participants, i * participants * 2 + participants * 2);
            if (tA.length === participants && tB.length === participants) {
                bracketsContainer.insertAdjacentHTML('beforeend', createMatchHtml(tA, tB, i + 1, mode));
            }
        }
    }

    function createMatchHtml(teamA, teamB, index, mode) {
        return `
            <div class="match-card bg-black/40 border border-white/5 rounded-xl overflow-hidden backdrop-blur-sm hover:border-[var(--primary)]/30 transition-all duration-500 animate-fade-in" style="animation-delay: ${index * 0.1}s">
                <div class="bg-white/5 px-3 py-1.5 flex justify-between items-center border-b border-white/5">
                    <span class="text-[8px] font-black text-[var(--primary)] tracking-widest uppercase">MATCH #${index}</span>
                    <span class="text-[8px] font-bold text-gray-500 uppercase">${mode}</span>
                </div>
                <div class="p-3 flex items-center justify-between gap-2 relative">
                    <div class="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 w-4 h-4 bg-black rounded-full border border-white/10 flex items-center justify-center text-[6px] text-white font-bold z-10">VS</div>
                    
                    <!-- Team A -->
                    <div class="flex-1 flex flex-col items-center gap-1">
                        <div class="flex -space-x-2">
                             ${teamA.map(a => `<img src="${a.imagenes.profile}" class="w-6 h-6 rounded-full border border-black grayscale object-cover" />`).join('')}
                        </div>
                        <span class="text-[7px] font-bold text-gray-400 uppercase truncate max-w-[60px]">${teamA[0].nombre}</span>
                    </div>

                    <!-- Team B -->
                    <div class="flex-1 flex flex-col items-center gap-1">
                        <div class="flex -space-x-2">
                             ${teamB.map(a => `<img src="${a.imagenes.profile}" class="w-6 h-6 rounded-full border border-black grayscale object-cover" />`).join('')}
                        </div>
                        <span class="text-[7px] font-bold text-gray-400 uppercase truncate max-w-[60px]">${teamB[0].nombre}</span>
                    </div>
                </div>
            </div>
        `;
    }

    if (regenerateBtn) {
        regenerateBtn.addEventListener('click', () => {
            const activeMode = document.querySelector('.mode-btn.active')?.getAttribute('data-mode') || '1v1';
            generateBrackets(activeMode);
        });
    }

    // FIGHT BUTTON
    const fightBtn = document.getElementById('fight-btn');
    fightBtn?.addEventListener('click', () => {
        const filledSlots = document.querySelectorAll('.team-slot[data-artist-id]').length;
        const totalNeeded = document.querySelectorAll('.team-slot').length;
        
        if (filledSlots < totalNeeded) {
            alert('¡Completa ambos equipos antes de la batalla!');
            return;
        }

        fightBtn.innerHTML = "<span class='animate-ping'>LUCHANDO...</span>";
        fightBtn.classList.add('bg-red-600', 'text-white', 'scale-110');
        
        // Shake screen effect
        const page = document.getElementById('page-container');
        page.classList.add('animate-shake');
        
        setTimeout(() => {
            page.classList.remove('animate-shake');
            fightBtn.innerHTML = "<span class='relative z-10 text-xl'>FIGHT!</span>";
            fightBtn.classList.remove('bg-red-600', 'text-white', 'scale-110');
            
            const winner = Math.random() > 0.5 ? 'TEAM ALPHA' : 'TEAM OMEGA';
            alert(`¡EL GANADOR ES ${winner}!`);
        }, 1500);
    });

    // Init
    generateBrackets('1v1');
    initSlots('1v1');

</script>

<style>
  /* UTILS */
  .mask-fade-edges {
    mask-image: linear-gradient(to right, transparent, black 5%, black 95%, transparent);
  }

  .custom-scrollbar::-webkit-scrollbar {
    height: 4px;
  }
  
  .custom-scrollbar::-webkit-scrollbar-track {
    background: rgba(255,255,255,0.05);
  }
  
  .custom-scrollbar::-webkit-scrollbar-thumb {
    background: var(--primary);
    border-radius: 10px;
  }

  @keyframes shake {
    0%, 100% { transform: translateX(0); }
    10%, 30%, 50%, 70%, 90% { transform: translateX(-5px); }
    20%, 40%, 60%, 80% { transform: translateX(5px); }
  }

  .animate-shake {
    animation: shake 0.5s cubic-bezier(.36,.07,.19,.97) both;
  }
  
  .arcade-font {
    font-family: 'Courier New', Courier, monospace; /* Fallback if no custom font */
  }
</style>
