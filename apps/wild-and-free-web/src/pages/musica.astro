---
import Layout from '../layouts/Layout.astro';
import tenantConfig from '../../../../packages/shared-types/tenant-config.json';

const tenant = tenantConfig['wild-and-free'];
const config = {
  name: "Wild Gvng - Música",
  primary_color: tenant.primary_color,
  secondary_color: tenant.secondary_color
};

// --- DATA MOCK (TODO EN ESPAÑOL) ---

const featuredAlbum = {
  title: "CRÓNICAS DEL ASFALTO",
  artist: "LIL KIRO x NEON GHOST",
  year: "2026",
  cover: "https://images.unsplash.com/photo-1614613535308-eb5fbd3d2c17?w=800&h=800&fit=crop",
  description: "Un viaje sonoro por las calles de la metrópolis.",
  tracks: [
    { title: "INTRO: LUCES", duration: "1:45" },
    { title: "VELOCIDAD LUZ", duration: "3:12" },
    { title: "DISTRITO 9", duration: "2:58" }
  ]
};

const artistsOfTheWeek = [
    { name: "LIL KIRO", role: "TRAP KING", image: "https://images.unsplash.com/photo-1493225457124-a3eb161ffa5f?w=300&h=300&fit=crop" },
    { name: "NEON GHOST", role: "DRILL MASTER", image: "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=300&h=300&fit=crop" },
    { name: "NOVA K", role: "R&B QUEEN", image: "https://images.unsplash.com/photo-1534528741775-53994a69daeb?w=300&h=300&fit=crop" }
];

const topCharts = [
  { rank: "01", title: "NOCHES DE NEÓN", artist: "LIL KIRO", plays: "2.1M", trend: "up", cover: "https://images.unsplash.com/photo-1493225457124-a3eb161ffa5f?w=100&h=100&fit=crop" },
  { rank: "02", title: "TOKYO DRIFT", artist: "NEON GHOST", plays: "1.8M", trend: "same", cover: "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=100&h=100&fit=crop" },
  { rank: "03", title: "FUEGO FATUO", artist: "LIL KIRO", plays: "1.5M", trend: "up", cover: "https://images.unsplash.com/photo-1534528741775-53994a69daeb?w=100&h=100&fit=crop" },
  { rank: "04", title: "SUEÑOS VAPOR", artist: "NEON GHOST", plays: "1.1M", trend: "down", cover: "https://images.unsplash.com/photo-1517841905240-472988babdf9?w=100&h=100&fit=crop" },
  { rank: "05", title: "JUNGLA URBANA", artist: "WILD SQUAD", plays: "950K", trend: "up", cover: "https://images.unsplash.com/photo-1531384441138-2736e62e0919?w=100&h=100&fit=crop" }
];

const recentlyAdded = [
    { name: "SHADOW V", date: "Hace 2h", image: "https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=100&h=100&fit=crop" },
    { name: "CRYSTAL WAVE", date: "Hace 5h", image: "https://images.unsplash.com/photo-1544005313-94ddf0286df2?w=100&h=100&fit=crop" },
    { name: "MIDNIGHT RIDER", date: "Ayer", image: "https://images.unsplash.com/photo-1500648767791-00dcc994a43e?w=100&h=100&fit=crop" },
    { name: "STORM TROOPER", date: "Ayer", image: "https://images.unsplash.com/photo-1506794778202-cad84cf45f1d?w=100&h=100&fit=crop" },
    { name: "VIPER X", date: "Hace 2d", image: "https://images.unsplash.com/photo-1615109398623-88346a601842?w=100&h=100&fit=crop" },
    { name: "LUNA NEGRA", date: "Hace 3d", image: "https://images.unsplash.com/photo-1517423568366-69755581d826?w=100&h=100&fit=crop" }
];

const allTracks = [
  { id: 1, title: 'NOCHES DE NEÓN', artist: 'LIL KIRO', album: 'LUCES DE CIUDAD', duration: '3:24', bpm: 128, key: 'Am', cover: 'https://images.unsplash.com/photo-1493225457124-a3eb161ffa5f?w=300&h=300&fit=crop' },
  { id: 2, title: 'TOKYO DRIFT', artist: 'NEON GHOST', album: 'SESIONES DE MEDIANOCHE', duration: '4:12', bpm: 140, key: 'F#', cover: 'https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=300&h=300&fit=crop' },
  { id: 3, title: 'REINO DE SOMBRAS', artist: 'SHADOW V', album: 'MATERIA OSCURA', duration: '3:58', bpm: 95, key: 'Cm', cover: 'https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=300&h=300&fit=crop' },
  { id: 4, title: 'CRISTALINO', artist: 'CRYSTAL WAVE', album: 'ONDAS', duration: '3:33', bpm: 110, key: 'G', cover: 'https://images.unsplash.com/photo-1544005313-94ddf0286df2?w=300&h=300&fit=crop' },
  { id: 5, title: 'PASEO DE MEDIANOCHE', artist: 'MIDNIGHT RIDER', album: 'JINETE NOCTURNO', duration: '4:01', bpm: 124, key: 'Dm', cover: 'https://images.unsplash.com/photo-1500648767791-00dcc994a43e?w=300&h=300&fit=crop' },
  { id: 6, title: 'FUEGO FATUO', artist: 'LIL KIRO', album: 'LUCES DE CIUDAD', duration: '2:55', bpm: 130, key: 'Em', cover: 'https://images.unsplash.com/photo-1534528741775-53994a69daeb?w=300&h=300&fit=crop' },
  { id: 7, title: 'SUEÑOS VAPOR', artist: 'NEON GHOST', album: 'SESIONES DE MEDIANOCHE', duration: '3:45', bpm: 100, key: 'Bm', cover: 'https://images.unsplash.com/photo-1517841905240-472988babdf9?w=300&h=300&fit=crop' },
  { id: 8, title: 'JUNGLA URBANA', artist: 'WILD SQUAD', album: 'MIXTAPE VOL.1', duration: '3:10', bpm: 145, key: 'Am', cover: 'https://images.unsplash.com/photo-1531384441138-2736e62e0919?w=300&h=300&fit=crop' },
  { id: 9, title: 'ALMA DE GRAFFITI', artist: 'STREET ARTIST', album: 'MUROS', duration: '4:20', bpm: 90, key: 'C#m', cover: 'https://images.unsplash.com/photo-1506794778202-cad84cf45f1d?w=300&h=300&fit=crop' },
  { id: 10, title: 'CONCRETO Y ACERO', artist: 'HEAVY HITTERS', album: 'CONSTRUCCIÓN', duration: '3:50', bpm: 135, key: 'Fm', cover: 'https://images.unsplash.com/photo-1529626455594-4ff0802cfb7e?w=300&h=300&fit=crop' }
];

// --- NUEVA DATA MOCK FREESTYLE ---

const ascensionData = {
    standings: [
        { rank: 1, name: "MC LÃSER", points: 2500, record: "15-2", lastBattle: "Win vs K-Road" },
        { rank: 2, name: "D-MENTE", points: 2350, record: "14-3", lastBattle: "Win vs Ziko" },
        { rank: 3, name: "K-ROAD", points: 2100, record: "12-5", lastBattle: "Loss vs Laser" },
        { rank: 4, name: "ZIKO", points: 1950, record: "11-6", lastBattle: "Loss vs D-Mente" },
    ],
    champions: [
        { date: "DIC 2025", winner: "MC LÃSER", event: "Final de Temporada" },
        { date: "NOV 2025", winner: "D-MENTE", event: "Rey del Parque" },
    ],
    videos: [
        { title: "FINAL: LÃSER VS D-MENTE", views: "125K", thumbnail: "https://images.unsplash.com/photo-1576158113928-4c240eaaf360?w=400&h=225&fit=crop" },
        { title: "SEMIFINAL: K-ROAD VS ZIKO", views: "89K", thumbnail: "https://images.unsplash.com/photo-1516280440614-6697288d5d38?w=400&h=225&fit=crop" },
    ],
    topTracks: [
        { title: "REY DEL PARQUE", artist: "MC LÃSER", plays: "45K" },
        { title: "SIN PIEDAD", artist: "D-MENTE", plays: "32K" },
        { title: "CALLEJÓN", artist: "K-ROAD", plays: "28K" },
    ]
};

const honorAndFreeData = [
    { 
        name: "MC LÃSER", 
        multiplier: "x1.5", 
        points: 450, 
        history: ["2026-02-15: +50pts (Ganador)", "2026-02-08: +30pts (Finalista)", "2026-02-01: +20pts (Asistencia)"], 
        image: "https://images.unsplash.com/photo-1570295999919-56ceb5ecca61?w=200&h=200&fit=crop",
        bio: "Campeón indiscutible de la temporada 2025. Conocido por su flow agresivo y punchlines directos. Representando a Torreón.",
        fullHistory: [
            { event: "Torneo de Verano '25", result: "Campeón", points: 1000 },
            { event: "Batalla del Desierto", result: "Semifinalista", points: 500 },
            { event: "Liga Invernal", result: "Campeón", points: 1200 }
        ]
    },
    { 
        name: "D-MENTE", 
        multiplier: "x1.2", 
        points: 380, 
        history: ["2026-02-15: +30pts (Finalista)", "2026-02-08: +50pts (Ganador)"], 
        image: "https://images.unsplash.com/photo-1535713875002-d1d0cf377fde?w=200&h=200&fit=crop",
        bio: "El estratega del verso. D-Mente destaca por sus métricas complejas y respuestas al momento. Representando a Gómez Palacio.",
        fullHistory: [
            { event: "Torneo de Verano '25", result: "Subcampeón", points: 800 },
            { event: "Batalla del Desierto", result: "Campeón", points: 1000 }
        ]
    },
    { 
        name: "K-ROAD", 
        multiplier: "x1.0", 
        points: 310, 
        history: ["2026-02-15: +10pts (Semis)", "2026-02-08: +10pts (Semis)"], 
        image: "https://images.unsplash.com/photo-1527980965255-d3b416303d12?w=200&h=200&fit=crop",
        bio: "La nueva sangre. K-Road ha subido rápidamente en los rankings gracias a su carisma y puesta en escena.",
        fullHistory: [
            { event: "Rookie del Año '25", result: "Ganador", points: 500 }
        ]
    },
    { 
        name: "ZIKO", 
        multiplier: "x1.0", 
        points: 290, 
        history: ["2026-02-15: +10pts (Semis)", "2026-02-01: +50pts (Ganador)"], 
        image: "https://images.unsplash.com/photo-1633332755192-727a05c4013d?w=200&h=200&fit=crop",
        bio: "Veterano de mil batallas. Ziko mantiene el estilo clásico del boom bap en sus improvisaciones.",
        fullHistory: [
            { event: "Leyendas Urbanas", result: "Cuartos", points: 300 }
        ]
    },
];

---

<Layout config={config}>
  <div class="min-h-screen bg-[var(--bg)] text-[var(--text)] pb-32 selection:bg-[var(--wild-orange)] selection:text-black font-sans">
    
    <!-- Top Bar -->
    <div class="bg-[var(--bg)] border-b border-[var(--border)] py-2 sticky top-0 z-50">
        <div class="container mx-auto px-4 max-w-[1800px] flex justify-between items-center">
            <div class="flex items-center gap-2">
                <div class="w-2 h-2 bg-[var(--wild-orange)] rounded-full animate-pulse"></div>
                <span class="text-[10px] font-black uppercase tracking-[0.2em] text-[var(--text)]">Wild Music OS v3.2</span>
            </div>
            <div class="flex gap-4 text-[10px] font-mono text-[var(--text-muted)]">
                <span>ESTADO: CONECTADO</span>
                <span>ZONA: GLOBAL</span>
            </div>
        </div>
    </div>

    <!-- SEARCH BAR (STICKY) -->
    <div class="sticky top-[33px] z-40 bg-[var(--bg)]-md border-b border-[var(--border)] py-4 shadow-2xl">
        <div class="container mx-auto px-4 max-w-[1800px] flex flex-col md:flex-row gap-4 justify-between items-center">
            <div class="relative w-full max-w-xl group">
                <input 
                    type="text" 
                    id="spotify-search" 
                    placeholder="BUSCAR ARTISTA O CANCIÓN EN SPOTIFY..." 
                    class="w-full bg-[var(--bg)] border border-[var(--border-strong)] text-[var(--text)] text-xs font-black uppercase tracking-widest py-3 pl-10 pr-4 rounded-full focus:outline-none focus:border-[var(--wild-orange)] focus:ring-1 focus:ring-[var(--wild-orange)] transition-all placeholder:text-[var(--text-muted)]"
                />
                <svg class="absolute left-4 top-1/2 -translate-y-1/2 text-[var(--text-muted)] group-focus-within:text-[var(--wild-orange)] transition-colors" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><circle cx="11" cy="11" r="8"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line></svg>
                
                <!-- Search Results Dropdown -->
                <div id="search-results" class="absolute top-full left-0 right-0 mt-2 bg-[var(--bg)] border border-[var(--border)] rounded-xl shadow-2xl overflow-hidden hidden max-h-[60vh] overflow-y-auto z-50">
                    <!-- Results injected here -->
                </div>
            </div>

            <!-- Filtros Rápidos -->
            <div class="flex items-center gap-2 overflow-x-auto no-scrollbar">
                <button class="px-4 py-1.5 text-[10px] font-black uppercase rounded bg-white text-black border border-[var(--border)] hover:bg-[var(--bg-hover)] transition-colors whitespace-nowrap">Todo</button>
                <button class="px-4 py-1.5 text-[10px] font-black uppercase rounded text-[var(--text-muted)] border border-[var(--border-strong)] hover:border-[var(--border)] hover:text-[var(--text)] transition-colors whitespace-nowrap">Hip Hop</button>
                <button class="px-4 py-1.5 text-[10px] font-black uppercase rounded text-[var(--text-muted)] border border-[var(--border-strong)] hover:border-[var(--border)] hover:text-[var(--text)] transition-colors whitespace-nowrap">Trap</button>
                <button class="px-4 py-1.5 text-[10px] font-black uppercase rounded text-[var(--text-muted)] border border-[var(--border-strong)] hover:border-[var(--border)] hover:text-[var(--text)] transition-colors whitespace-nowrap">Drill</button>
            </div>
        </div>
    </div>

    <!-- MAIN LAYOUT -->
    <div class="container mx-auto px-4 py-8 max-w-[1800px] animate-fade-in-up space-y-16">
      
      <!-- 1. AGREGADOS RECIENTEMENTE (Horizontal List) - AHORA ARRIBA -->
      <section>
          <div class="flex items-center gap-4 mb-6">
              <h3 class="text-xl font-black uppercase tracking-tighter text-[var(--text)]">Agregados Recientemente a tu Playlist</h3>
              <div class="h-px bg-[var(--bg-card)] flex-1"></div>
              <a href="#" class="text-[10px] font-black text-[var(--wild-orange)] uppercase hover:underline">Ver Historial</a>
          </div>

          <div class="flex gap-6 overflow-x-auto pb-4 scrollbar-hide">
              {recentlyAdded.map(artist => (
                  <div class="flex-shrink-0 flex items-center gap-3 bg-[var(--bg)] border border-[var(--border)] pr-6 pl-2 py-2 rounded-full hover:border-[var(--wild-orange)] transition-colors cursor-pointer group min-w-[200px]">
                      <img src={artist.image} width="40" height="40" loading="lazy" decoding="async" class="w-10 h-10 rounded-full object-cover border border-[var(--border-strong)] group-hover:border-[var(--wild-orange)]" />
                      <div>
                          <h4 class="text-xs font-black text-[var(--text)] uppercase leading-none mb-1">{artist.name}</h4>
                          <span class="text-[9px] font-mono text-[var(--text-muted)] uppercase block">{artist.date}</span>
                      </div>
                  </div>
              ))}
          </div>
      </section>

      <!-- 2. SECCIÓN DESTACADOS: GRID 3 COLUMNAS -->
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
          
          <!-- COL 1: ÃLBUM DE LA SEMANA -->
          <div class="relative group overflow-hidden rounded-xl border border-[var(--border)] bg-[var(--bg)] min-h-[400px]">
                <div class="absolute inset-0  mix-blend-overlay"></div>
                <div class="absolute inset-0 bg-[var(--bg)] z-10"></div>
                <img src={featuredAlbum.cover} width="400" height="400" loading="lazy" decoding="async" class="absolute inset-0 w-full h-full object-cover opacity-60 group-hover:opacity-80 transition-opacity duration-700" />
                
                <div class="absolute inset-0 p-6 z-20 flex flex-col justify-end items-start">
                    <span class="inline-block bg-[var(--wild-orange)] text-[var(--text)] text-[9px] font-black uppercase px-2 py-1 mb-2">Ãlbum de la Semana</span>
                    <h2 class="text-3xl font-black uppercase italic tracking-tighter leading-none mb-1 text-[var(--text)] drop-shadow-lg">{featuredAlbum.title}</h2>
                    <h3 class="text-lg text-[var(--text-secondary)] font-bold uppercase mb-4 tracking-widest">{featuredAlbum.artist}</h3>
                    
                    <button class="bg-white text-black px-4 py-2 text-[10px] font-black uppercase hover:bg-[var(--wild-orange)] hover:text-[var(--text)] transition-colors flex items-center gap-2">
                        <svg width="12" height="12" viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg> Escuchar
                    </button>
                </div>
          </div>

          <!-- COL 2: ARTISTA DE LA SEMANA -->
          <div class="bg-[var(--bg)] border border-[var(--border)] rounded-xl p-6 flex flex-col">
              <div class="flex justify-between items-center mb-6 border-b border-[var(--border)] pb-4">
                  <h3 class="text-lg font-black uppercase tracking-tighter text-[var(--text)]">Artistas Semana</h3>
                  <span class="text-[9px] font-mono text-[var(--wild-orange)] uppercase">Top 3</span>
              </div>
              
              <div class="flex-1 flex flex-col gap-3 justify-center">
                  {artistsOfTheWeek.map((artist, i) => (
                      <div class="group flex items-center gap-4 p-3 bg-[var(--bg-card)] hover:bg-[var(--bg-card)] rounded-lg transition-all cursor-pointer border border-transparent hover:border-[var(--wild-orange)]/30">
                          <span class="text-xl font-black text-[var(--text-faint)] font-mono w-6 text-center group-hover:text-[var(--wild-orange)] transition-colors">0{i+1}</span>
                          <img src={artist.image} width="40" height="40" loading="lazy" decoding="async" class="w-10 h-10 rounded-full object-cover grayscale group-hover:grayscale-0 ring-1 ring-transparent group-hover:ring-[var(--wild-orange)] transition-all" />
                          <div class="flex-1 min-w-0">
                              <h4 class="text-sm font-black text-[var(--text)] uppercase leading-none mb-1 group-hover:text-[var(--wild-orange)] transition-colors truncate">{artist.name}</h4>
                              <p class="text-[9px] font-mono text-[var(--text-muted)] uppercase tracking-wider truncate">{artist.role}</p>
                          </div>
                      </div>
                  ))}
              </div>
          </div>

          <!-- COL 3: TOP 5 LAGUNA (PEQUEÑO) -->
          <div class="bg-[var(--bg)] border border-[var(--border)] rounded-xl p-6 flex flex-col">
              <div class="flex justify-between items-center mb-6 border-b border-[var(--border)] pb-4">
                  <h3 class="text-lg font-black uppercase tracking-tighter text-[var(--text)]">Top 5 Laguna</h3>
                  <span class="text-[9px] font-mono text-green-500 uppercase animate-pulse">â— En Vivo</span>
              </div>
              
              <div class="flex-1 flex flex-col gap-2 overflow-y-auto">
                  {topCharts.map((track, _i) => (
                      <div class="flex items-center gap-3 p-2 hover:bg-[var(--bg-card)] rounded transition-colors group cursor-pointer">
                          <div class="w-6 h-6 flex items-center justify-center bg-[var(--bg-card)] rounded text-[10px] font-black text-[var(--text)] group-hover:bg-[var(--wild-orange)] group-hover:text-black transition-colors">
                              {track.rank}
                          </div>
                          <div class="flex-1 min-w-0">
                              <h4 class="text-xs font-bold text-[var(--text)] uppercase truncate">{track.title}</h4>
                              <p class="text-[9px] text-[var(--text-muted)] uppercase truncate">{track.artist}</p>
                          </div>
                          <span class={`text-[9px] font-black ${track.trend === 'up' ? 'text-green-500' : 'text-[var(--text-muted)]'}`}>
                              {track.trend === 'up' ? 'â–²' : '-'}
                          </span>
                      </div>
                  ))}
              </div>
          </div>
      </div>

      <!-- 3. CATÃLOGO MAESTRO (ANCHO COMPLETO) -->
      <div class="bg-[var(--bg)] border border-[var(--border)] rounded-xl overflow-hidden shadow-2xl">
         <div class="p-6 border-b border-[var(--border)] flex flex-col md:flex-row justify-between items-center gap-4 bg-[var(--bg-card)]">
             <div class="flex items-center gap-4">
                 <div class="p-2 bg-[var(--wild-orange)] rounded text-black">
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><circle cx="12" cy="12" r="10"></circle><circle cx="12" cy="12" r="3"></circle></svg>
                 </div>
                 <div>
                    <h2 class="text-2xl font-black uppercase tracking-tight text-[var(--text)] leading-none">Catálogo Maestro</h2>
                    <span class="text-[10px] font-mono text-[var(--text-muted)] uppercase tracking-widest">Base de Datos Completa /// {allTracks.length} Entradas</span>
                 </div>
             </div>
             <div class="flex gap-2 w-full md:w-auto">
                 <input type="text" placeholder="BUSCAR..." class="bg-[var(--bg)] border border-[var(--border-strong)] text-[var(--text)] px-4 py-2 text-xs font-mono w-full md:w-64 focus:outline-none focus:border-[var(--wild-orange)] uppercase rounded" />
                 <button class="bg-[var(--bg-card)] hover:bg-[var(--bg-hover)]/20 text-[var(--text)] px-4 py-2 rounded border border-[var(--border)] transition-colors">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="8" y1="6" x2="21" y2="6"></line><line x1="8" y1="12" x2="21" y2="12"></line><line x1="8" y1="18" x2="21" y2="18"></line><line x1="3" y1="6" x2="3.01" y2="6"></line><line x1="3" y1="12" x2="3.01" y2="12"></line><line x1="3" y1="18" x2="3.01" y2="18"></line></svg>
                 </button>
             </div>
         </div>

         <!-- Header Tabla -->
         <div class="grid grid-cols-12 gap-4 px-6 py-3 bg-[var(--bg)] text-[10px] font-bold text-[var(--text-muted)] uppercase tracking-widest border-b border-[var(--border-faint)]">
             <div class="col-span-1">#</div>
             <div class="col-span-5 md:col-span-4">Título / Artista</div>
             <div class="col-span-2 hidden md:block">Ãlbum</div>
             <div class="col-span-2 hidden md:block text-center">Datos</div>
             <div class="col-span-2 hidden md:block text-right">Duración</div>
             <div class="col-span-2 md:col-span-1 text-right"></div>
         </div>

         <!-- Filas -->
         <div class="divide-y divide-[var(--border-faint)] bg-[var(--bg)]">
             {allTracks.map((track) => (
                 <div class="grid grid-cols-12 gap-4 px-6 py-3 items-center hover:bg-[var(--bg-card)] transition-colors group cursor-pointer text-xs">
                     <div class="col-span-1 font-mono text-[var(--text-muted)] group-hover:text-[var(--wild-orange)]">{track.id.toString().padStart(3, '0')}</div>
                     
                     <div class="col-span-5 md:col-span-4 flex items-center gap-3">
                         <div class="w-10 h-10 bg-[var(--bg-card)] rounded overflow-hidden flex-shrink-0 relative">
                             <img src={track.cover} width="40" height="40" loading="lazy" decoding="async" class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all" />
                             <div class="absolute inset-0 bg-[var(--bg)]/50 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity">
                                <svg width="12" height="12" viewBox="0 0 24 24" fill="white"><path d="M8 5v14l11-7z"/></svg>
                             </div>
                         </div>
                         <div class="min-w-0">
                             <div class="font-bold text-[var(--text)] uppercase truncate group-hover:text-[var(--wild-orange)] transition-colors">{track.title}</div>
                             <div class="text-[10px] text-[var(--text-muted)] truncate uppercase">{track.artist}</div>
                         </div>
                     </div>
                     
                     <div class="col-span-2 hidden md:block text-[var(--text-muted)] text-[10px] uppercase truncate">{track.album}</div>
                     
                     <div class="col-span-2 hidden md:block text-center font-mono text-[var(--text-muted)] text-[10px]">
                         <span class="bg-[var(--bg-card)] px-2 py-1 rounded border border-[var(--border-faint)]">{track.bpm} BPM</span>
                     </div>
                     
                     <div class="col-span-2 hidden md:block text-right font-mono text-[var(--text-muted)] text-[10px]">{track.duration}</div>
                     
                     <div class="col-span-2 md:col-span-1 flex justify-end">
                         <button 
                             class="play-track-btn w-8 h-8 hover:bg-[var(--wild-orange)] text-[var(--text-muted)] hover:text-[var(--text)] rounded-full flex items-center justify-center transition-colors"
                             data-title={track.title}
                             data-artist={track.artist}
                             data-cover={track.cover}
                             data-duration={track.duration}
                         >
                             <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg>
                         </button>
                     </div>
                 </div>
             ))}
         </div>
         
         <div class="p-3 bg-[var(--bg)]/50 border-t border-[var(--border-faint)] text-center">
             <button class="text-[10px] font-bold uppercase text-[var(--text-muted)] hover:text-[var(--text)] transition-colors">Cargar Más Entradas</button>
         </div>
      </div>

      <!-- 4. ASCENSIÓN (FREESTYLE TOURNAMENT) -->
      <section>
          <div class="flex items-center gap-4 mb-8">
              <h2 class="text-3xl font-black uppercase tracking-tighter text-[var(--text)]">Ascensión</h2>
              <span class="bg-red-600 text-[var(--text)] text-[10px] font-black uppercase px-2 py-0.5 rounded animate-pulse">En Vivo</span>
              <div class="h-px bg-[var(--bg-card)] flex-1"></div>
          </div>

          <div class="grid grid-cols-1 lg:grid-cols-12 gap-8">
              <!-- Clasificados -->
              <div class="lg:col-span-4 bg-[var(--bg)] border border-[var(--border)] rounded-xl p-6">
                  <h3 class="text-xl font-black uppercase mb-6 text-[var(--text)]">Clasificación</h3>
                  <div class="space-y-4">
                      {ascensionData.standings.map((mc, _i) => (
                          <div class="flex items-center justify-between p-3 bg-[var(--bg-card)] rounded-lg border border-transparent hover:border-red-600/50 transition-colors">
                              <div class="flex items-center gap-3">
                                  <span class="text-lg font-black text-[var(--text-muted)] w-6">#{mc.rank}</span>
                                  <div>
                                      <h4 class="text-sm font-black text-[var(--text)] uppercase">{mc.name}</h4>
                                      <p class="text-[9px] text-[var(--text-muted)] uppercase">{mc.lastBattle}</p>
                                  </div>
                              </div>
                              <div class="text-right">
                                  <span class="block text-lg font-black text-[var(--wild-orange)]">{mc.points}</span>
                                  <span class="text-[9px] text-[var(--text-muted)] font-mono">PTS • {mc.record}</span>
                              </div>
                          </div>
                      ))}
                  </div>
              </div>

              <!-- Historial & Videos -->
              <div class="lg:col-span-8 space-y-8">
                  <!-- Videos -->
                  <div>
                      <h3 class="text-lg font-black uppercase mb-4 text-[var(--text)]">Últimas Batallas</h3>
                      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                          {ascensionData.videos.map(video => (
                              <div class="group relative aspect-video bg-[var(--bg)] rounded-xl overflow-hidden cursor-pointer border border-[var(--border)]">
                                  <img src={video.thumbnail} width="400" height="225" loading="lazy" decoding="async" class="w-full h-full object-cover opacity-60 group-hover:opacity-80 transition-opacity" />
                                  <div class="absolute inset-0 flex items-center justify-center">
                                      <div class="w-12 h-12 rounded-full bg-red-600/80 text-[var(--text)] flex items-center justify-center group-hover:scale-110 transition-transform">
                                          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg>
                                      </div>
                                  </div>
                                  <div class="absolute bottom-0 left-0 right-0 p-4 bg-[var(--bg)]">
                                      <h4 class="text-sm font-black text-[var(--text)] uppercase truncate">{video.title}</h4>
                                      <p class="text-[10px] text-[var(--text-muted)] font-mono uppercase">{video.views} Vistas</p>
                                  </div>
                              </div>
                          ))}
                      </div>
                  </div>

                  <!-- Rolas del Artista (Tracks Destacados) -->
                  <div>
                      <h3 class="text-lg font-black uppercase mb-4 text-[var(--text)]">Tracks Destacados</h3>
                      <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                          {ascensionData.topTracks.map(track => (
                              <div class="flex items-center gap-3 p-3 bg-[var(--bg-card)] rounded-lg border border-[var(--border-faint)] hover:border-[var(--wild-orange)] transition-colors group cursor-pointer">
                                  <div class="w-8 h-8 bg-[var(--bg-card)] flex items-center justify-center rounded text-[var(--wild-orange)]">
                                      <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M9 18V5l12-2v13"/></svg>
                                  </div>
                                  <div>
                                      <h4 class="text-xs font-black text-[var(--text)] uppercase">{track.title}</h4>
                                      <p class="text-[9px] text-[var(--text-muted)] uppercase">{track.artist}</p>
                                  </div>
                              </div>
                          ))}
                      </div>
                  </div>
              </div>
          </div>
      </section>

      <!-- 5. HONOR AND FREE (TABLA DE PUNTOS) -->
      <section>
          <div class="flex items-center gap-4 mb-8">
              <h2 class="text-3xl font-black uppercase tracking-tighter text-[var(--text)]">Honor & Free</h2>
              <div class="h-px bg-[var(--bg-card)] flex-1"></div>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
              {honorAndFreeData.map(mc => (
                  <div class="freestyler-card bg-[var(--bg)] border border-[var(--border)] rounded-xl overflow-hidden group hover:border-[var(--wild-orange)] transition-colors" data-freestyler={JSON.stringify(mc)}>
                      <div class="relative h-48 overflow-hidden cursor-pointer freestyler-trigger">
                          <img src={mc.image} width="300" height="300" loading="lazy" decoding="async" class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-500 group-hover:scale-105" />
                          <div class="absolute inset-0 bg-[var(--bg)]"></div>
                          <div class="absolute inset-0 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity z-10">
                              <span class="bg-[var(--bg)] text-[var(--wild-orange)] text-[10px] font-black uppercase px-3 py-1 rounded border border-[var(--wild-orange)] transform translate-y-2 group-hover:translate-y-0 transition-transform">Ver Perfil</span>
                          </div>
                          <div class="absolute bottom-4 left-4 z-20 pointer-events-none">
                              <h3 class="text-2xl font-black text-[var(--text)] uppercase italic leading-none">{mc.name}</h3>
                              <span class="text-[10px] font-mono text-[var(--wild-orange)] bg-[var(--bg)]/50 px-2 py-0.5 rounded">MULT: {mc.multiplier}</span>
                          </div>
                      </div>
                      
                      <div class="p-4 bg-[var(--bg)]">
                          <div class="flex justify-between items-end mb-4">
                              <span class="text-[10px] text-[var(--text-muted)] font-black uppercase tracking-widest">Puntos Totales</span>
                              <span class="text-3xl font-black text-[var(--text)] leading-none">{mc.points}</span>
                          </div>
                          
                          <details class="group/details">
                              <summary class="cursor-pointer text-[10px] font-black uppercase text-[var(--text-muted)] hover:text-[var(--text)] flex items-center justify-between p-2 bg-[var(--bg)] rounded select-none">
                                  <span>Ver Historial</span>
                                  <svg class="w-4 h-4 transition-transform group-open/details:rotate-180" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 9l6 6 6-6"/></svg>
                              </summary>
                              <div class="mt-2 pl-2 border-l border-[var(--border)] space-y-1">
                                  {mc.history.map(entry => (
                                      <p class="text-[9px] font-mono text-[var(--text-muted)] uppercase">{entry}</p>
                                  ))}
                              </div>
                          </details>
                      </div>
                  </div>
              ))}
          </div>
      </section>

    </div>

    <!-- MODAL DE FREESTYLER -->
    <dialog id="freestyler-modal" class="bg-transparent p-0 backdrop:bg-[var(--bg)] w-full max-w-2xl m-auto open:animate-fade-in-up">
        <div class="bg-[var(--bg)] border border-[var(--border-strong)] rounded-xl overflow-hidden shadow-2xl">
            <!-- Header con Imagen de fondo -->
            <div class="relative h-48">
                <img id="modal-image" src="" width="800" height="600" class="w-full h-full object-cover opacity-50" />
                <div class="absolute inset-0 bg-[var(--bg)]"></div>
                <div class="absolute bottom-6 left-6">
                    <h2 id="modal-name" class="text-5xl font-black text-[var(--text)] uppercase italic tracking-tighter leading-none">NAME</h2>
                    <p id="modal-bio" class="text-sm text-[var(--text-secondary)] max-w-lg mt-2 font-medium"></p>
                </div>
                <button id="close-modal" class="absolute top-4 right-4 text-[var(--text)] hover:text-[var(--wild-orange)] bg-[var(--bg)]/50 p-2 rounded-full transition-colors">
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg>
                </button>
            </div>
            
            <!-- Contenido -->
            <div class="p-8">
                <h3 class="text-xl font-black uppercase text-[var(--wild-orange)] mb-4">Historial Completo de Torneo</h3>
                <div class="overflow-x-auto">
                    <table class="w-full text-left">
                        <thead>
                            <tr class="text-[10px] font-black uppercase text-[var(--text-muted)] border-b border-[var(--border)]">
                                <th class="pb-2">Evento</th>
                                <th class="pb-2">Resultado</th>
                                <th class="pb-2 text-right">Puntos</th>
                            </tr>
                        </thead>
                        <tbody id="modal-history-body" class="divide-y divide-[var(--border-faint)]">
                            <!-- JS populated -->
                        </tbody>
                    </table>
                </div>
                
                <div class="mt-8 flex gap-4">
                     <button class="flex-1 bg-[var(--wild-orange)] text-black font-black uppercase py-3 text-xs rounded hover:bg-[var(--bg-hover)] transition-colors">Ver Perfil Completo</button>
                     <button class="flex-1 border border-[var(--border-strong)] text-[var(--text)] font-black uppercase py-3 text-xs rounded hover:bg-[var(--bg-hover)] hover:text-black transition-colors">Seguir Freestyler</button>
                </div>
            </div>
        </div>
    </dialog>

    <!-- Floating Player Removed (Replaced by Global Spotify Player) -->
  </div>
</Layout>

<style is:global define:vars={{ streetAccent: tenant.primary_color }}>
  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }
  .animate-fade-in-up {
    animation: fadeInUp 0.5s cubic-bezier(0.4, 0, 0.2, 1) forwards;
  }
  
  .scrollbar-hide::-webkit-scrollbar {
      display: none;
  }
  .scrollbar-hide {
      -ms-overflow-style: none;
      scrollbar-width: none;
  }
</style>

<script>
    import { Favorites } from '../lib/favorites.js';

    const searchInput = document.getElementById('spotify-search') as HTMLInputElement;
    const searchResults = document.getElementById('search-results');
    let debounceTimer: any;

    searchInput?.addEventListener('input', (e) => {
        const query = (e.target as HTMLInputElement).value;
        
        clearTimeout(debounceTimer);
        
        if (query.length < 2) {
            searchResults?.classList.add('hidden');
            return;
        }

        debounceTimer = setTimeout(async () => {
            try {
                // Connect to Core Engine (Go)
                // Use PUBLIC_CORE_API_URL or fallback to localhost
                const API_URL = import.meta.env.PUBLIC_CORE_API_URL || 'http://localhost:8080';
                
                const res = await fetch(`${API_URL}/api/v1/spotify/search?q=${encodeURIComponent(query)}`);
                const data = await res.json();
                
                if (data.tracks && data.tracks.items.length > 0) {
                    renderResults(data.tracks.items);
                } else {
                    if (searchResults) {
                        searchResults.innerHTML = '<div class="p-4 text-center text-[var(--text-muted)] text-xs font-bold uppercase">No se encontraron resultados</div>';
                        searchResults.classList.remove('hidden');
                    }
                }
            } catch (err) {
                console.error('Error searching:', err);
            }
        }, 500);
    });

    function renderResults(tracks: any[]) {
        if (!searchResults) return;
        
        const html = tracks.map(track => {
            const image = track.album.images[2]?.url || track.album.images[0]?.url;
            const artist = track.artists.map((a: any) => a.name).join(', ');
            // Escape quotes for data attributes
            const safeTrack = JSON.stringify({
                id: track.id,
                title: track.name,
                artist: artist,
                image: image
            }).replace(/"/g, '&quot;');
            
            const isFav = Favorites.has(track.id);
            const favClass = isFav ? 'text-[var(--wild-orange)] fill-current' : 'text-[var(--text-muted)] hover:text-[var(--text)]';
            const fillAttr = isFav ? 'currentColor' : 'none';

            return `
            <div class="flex items-center gap-4 p-3 hover:bg-[var(--bg-card)] cursor-pointer border-b border-[var(--border-faint)] last:border-0 transition-colors group relative" onclick="window.playSpotifyTrack('${track.id}')">
                <div class="w-10 h-10 rounded overflow-hidden relative flex-shrink-0">
                    <img src="${image}" width="40" height="40" loading="lazy" decoding="async" class="w-full h-full object-cover" />
                    <div class="absolute inset-0 bg-[var(--bg)]/50 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="white"><path d="M8 5v14l11-7z"/></svg>
                    </div>
                </div>
                <div class="flex-1 min-w-0 pr-8">
                    <h4 class="text-xs font-black text-[var(--text)] uppercase truncate">${track.name}</h4>
                    <p class="text-[10px] text-[var(--text-muted)] font-bold truncate">${artist}</p>
                </div>
                <button 
                    class="fav-track-btn absolute right-3 top-1/2 -translate-y-1/2 p-2 transition-colors z-10 ${favClass}"
                    data-track="${safeTrack}"
                    onclick="event.stopPropagation(); window.toggleTrackFavorite(this, '${track.id}')"
                >
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="${fillAttr}" stroke="currentColor" stroke-width="2"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/></svg>
                </button>
            </div>
            `;
        }).join('');
        
        searchResults.innerHTML = html;
        searchResults.classList.remove('hidden');
    }

    (window as any).toggleTrackFavorite = async (btn: HTMLElement, _trackId: string) => {
        const trackData = JSON.parse(btn.dataset.track || '{}');
        const isFav = await Favorites.toggle(trackData);
        
        const svg = btn.querySelector('svg');
        if (isFav) {
            btn.className = "fav-track-btn absolute right-3 top-1/2 -translate-y-1/2 p-2 transition-colors z-10 text-[var(--wild-orange)] fill-current";
            svg?.setAttribute('fill', 'currentColor');
        } else {
            btn.className = "fav-track-btn absolute right-3 top-1/2 -translate-y-1/2 p-2 transition-colors z-10 text-[var(--text-muted)] hover:text-[var(--text)]";
            svg?.setAttribute('fill', 'none');
        }
    };

    (window as any).playSpotifyTrack = (id: string) => {
        window.dispatchEvent(new CustomEvent('play-spotify-track', { detail: { id } }));
        if (searchResults) searchResults.classList.add('hidden');
        if (searchInput) searchInput.value = '';
    };

    document.addEventListener('click', (e) => {
        if (searchInput && searchResults && !searchInput.contains(e.target as Node) && !searchResults.contains(e.target as Node)) {
            searchResults.classList.add('hidden');
        }
    });

  // Player Logic (Replaced by Global Spotify Player)
  const playBtns = document.querySelectorAll('.play-track-btn');
  
  if (playBtns) {
    playBtns.forEach(btn => {
      btn.addEventListener('click', (e) => {
        e.stopPropagation();
        // Play default track or random one since we don't have IDs for mock data
        // Ideally we would map mock titles to Spotify IDs
        (window as any).playSpotifyTrack('4cOdK2wGLETKBW3PvgPWqT'); 
      });
    });
  }

  // Freestyler Modal Logic
  const modal = document.getElementById('freestyler-modal') as HTMLDialogElement;
  const closeModalBtn = document.getElementById('close-modal');
  const triggers = document.querySelectorAll('.freestyler-trigger');

  if (modal && closeModalBtn) {
      triggers.forEach(trigger => {
          trigger.addEventListener('click', (e) => {
              e.stopPropagation(); // Prevent details/summary conflict if any
              const card = trigger.closest('.freestyler-card') as HTMLElement;
              const data = JSON.parse(card.dataset.freestyler || '{}');
              
              // Populate
              const nameEl = document.getElementById('modal-name');
              const bioEl = document.getElementById('modal-bio');
              const imgEl = document.getElementById('modal-image') as HTMLImageElement;
              const historyBody = document.getElementById('modal-history-body');
              
              if (nameEl) nameEl.textContent = data.name;
              if (bioEl) bioEl.textContent = data.bio || 'Sin biografía disponible.';
              if (imgEl) imgEl.src = data.image;
              
              if (historyBody && data.fullHistory) {
                  historyBody.innerHTML = data.fullHistory.map((h: any) => `
                      <tr class="text-xs hover:bg-[var(--bg-card)] transition-colors">
                          <td class="py-3 font-bold text-[var(--text)] uppercase">${h.event}</td>
                          <td class="py-3 text-[var(--text-muted)] uppercase">${h.result}</td>
                          <td class="py-3 text-right font-mono text-[var(--wild-orange)]">${h.points} PTS</td>
                      </tr>
                  `).join('');
              } else if (historyBody) {
                  historyBody.innerHTML = '<tr><td colspan="3" class="py-4 text-center text-[var(--text-muted)] text-xs">No hay historial disponible</td></tr>';
              }

              modal.showModal();
          });
      });

      closeModalBtn.addEventListener('click', () => {
          modal.close();
      });

      modal.addEventListener('click', (e) => {
          const rect = modal.getBoundingClientRect();
          // Check if click is outside
          const isOutside = (
              e.clientY < rect.top ||
              e.clientY > rect.bottom ||
              e.clientX < rect.left ||
              e.clientX > rect.right
          );
          if (isOutside) {
              modal.close();
          }
      });
  }
</script>