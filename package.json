{
  "name": "musicians-hub-root",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "dev": "pnpm -r --parallel --if-present run dev"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ]
}
