{
  "framework": "astro",
  "installCommand": "corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "if [ -d apps/wild-and-free-web ]; then pnpm --filter wild-and-free-web build && rm -rf dist && cp -R apps/wild-and-free-web/dist dist; else pnpm build && rm -rf ../dist && cp -R dist ../dist; fi",
  "outputDirectory": "dist",
  "github": {
    "silent": true
  }
}
